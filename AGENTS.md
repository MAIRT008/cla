# AI 开发与评审入口

> 此文件是项目交接索引和已确认约束的摘录，不创建新的控制平台。当前已有局部离线实现；完整四模块仍在开发，不能据局部 PASS 报告软件全部完成。

## 当前项目分工与文档位置

**2026-09-21 当前安排：** Owner 已要求继续补齐完整产品，施工单交给 Owner 当前使用的 **OPUS**，由 Owner 启动并带回报告；当前验收 Agent（本任务）负责发布施工任务、独立复核和技术裁决。RC1—RC5 已按离线阶段关闭：RC3 经六轮限定复核于 2026-09-17 PASS（[Round 6 交接](evidence/development/rc3-fix-round6-handoff.md)）；RC4 于 2026-09-17 Round 2 PASS、总体 PASS WITH DEBT（[Round 2 交接](evidence/development/rc4-round2-handoff.md)）；RC5 于 2026-09-21 Round 2 限定复核 PASS WITH DEBT（[Round 2 交接](evidence/development/rc5-round2-handoff.md)）。这些通过只代表源码与项目内离线阶段：三个 Rust crate 未编译/运行，E01—E52 真实实验均未执行。当前项是 RC6（发布候选交付与全链实现复核）：OPUS 已按[施工单](evidence/development/opus-rc6-release-candidate-task.md)施工（[交接](evidence/development/rc6-handoff.md)），RC6 Round 1 复核 FAIL（1 BLOCK、3 MAJOR、1 MINOR）；Round 2 复核 FAIL（2 BLOCK、1 MAJOR，[Round 2 交接](evidence/development/rc6-round2-handoff.md)）；Round 3 复核 FAIL（2 BLOCK、1 MAJOR，[Round 3 交接](evidence/development/rc6-round3-handoff.md)）；Round 4 限定整改（[Round 4 交接](evidence/development/rc6-round4-handoff.md)，Codex 裁决扩到 `core/network`）于 2026-09-21 复核 PASS WITH DEBT，不需要 Round 5；「阻断删到一半失败」记为 NOTE、作为 E56 验收重点。RC6 仍 `NOT_READY`、未关闭：Rust 与 NSIS 未编译运行，E53—E58 未执行，Mihomo 固定哈希与签名条件未提供；下一步由 Owner 准备构建机与隔离验收机，完成 Rust/NSIS 构建和 E53—E58。连续范围见 [完整产品补齐计划](evidence/development/product-completion-plan.md)。不自动另建施工 Agent 或恢复定时调度。此前 CLI GROK 与 Spark 的阶段交接保留为历史，不再把 GROK T5 写成当前下一单。

施工方负责复用选型试验、实现、自测与整改，开始单项工作时明确文件范围，避免相互覆盖。验收 Agent 根据实际代码和运行证据裁决技术取舍、缺陷等级与阶段通过/返工，原则上不代写被验收的产品实现。改变 Owner 已确认的目标、范围、业务规则或新增采购/部署授权，仍交 Owner 决定；日常技术判断不反复上交。

正式文档直接位于本项目的 README.md、ARCHITECTURE.md 和 DOCS/，从当前工作树读取和修订。dist/ 下的 ZIP 及其展开目录只是历史导出副本，不是另一套方案权威，不需要解压后才能开工；后续以项目内当前源文档为准。

## 当前开工授权与本机保护

Owner 已授权项目内开发。当前按上文的 RC1—RC6 补齐客户端、完整管理后台、原生服务与内核、真实采集/执行代码、日志和安装交付。早期授权记录见 [施工边界](evidence/p0/owner-scope.md)，当前以 [完整产品补齐计划](evidence/development/product-completion-plan.md) 为准；历史“仅文档授权／全部未开工”不表示当前状态，已有文档校验也不构成产品完成证据。

Owner 最新持续目标：**先完整实现四大功能及界面，复用成熟现成能力，项目内模拟测试随开发执行；所有真实测试与实验留到项目最后，在另行批准的环境执行。** 不得将小样机或单模块通过当作整项目停点；继续推进其余无实测依赖的实现。产品代码、模拟验证和实机验证分别记录，整体进度见 [连续施工计划](evidence/development/implementation-plan.md)。本目标不授权本机安装、环境改动、真实账号处理或真实外部服务调用。

**2026-09-16 顺序纠正：** 本机同时运行其他项目，稳定网络与运行环境是硬边界。先完成项目内实现和离线模拟，最后由 Owner 安排其他电脑统一验收；启动、安装和业务报错保存在该测试电脑的本地日志，集中整改后异机复测。E01 仍是异机原生验证首步，不能再被用作整个项目唯一下一步或缺失代码的停工理由。原方案已有服务端职责不削减；已确认采用 Rust 控制端、同机回环测试部署和首启自行设置管理员账号密码，不等于把多用户与服务端配额降成单机内存状态。

**不得破坏或调整 Owner 当前网络配置与运行环境。任何真实网络测试，必须事先列明具体操作、目标机器、影响和恢复方式，得到 Owner 明确批准才可执行；“开工”不是该项批准。** 当前只允许项目范围内的实现、合成数据和离线验证。不得启动代理内核、安装/启停系统服务、改动 TUN/代理/DNS/防火墙/路由或浏览器真实 Profile，不访问运行中的代理控制接口，不做出口、DNS/STUN、延迟/连通性、故障注入或真实配额探测，不调用真实模型服务。环境变更也需先取得相应授权。

公开文档与源码的普通读取不等于实网测试，但不能借此修复代理、修改证书或升级现有环境。实网依赖未就绪不阻止独立的界面和本地功能施工；演示数据须可辨识，未接入能力保持未接入／未验证，不能返回伪造成功。

## 先读什么

先读 [README](README.md)、[首版范围](DOCS/RELEASE_SCOPE.md)、[ARCHITECTURE](ARCHITECTURE.md)。然后按具体任务读取 [模块索引](DOCS/DOCUMENT_INDEX.md) 所列 FD 的完整相关章节与验收场景；不要只看摘要或海报开始实施。

任务必须写清所引用的编号，例如 `FD-02/N18`、`FD-03/F29`、`FD-04/A01`。FD-02 的功能前缀是 N，其他三个模块是 F；不要重新编号原文。

## 不得擅自改变的内容

1. 首版不建隔离工作台、不要求安装 WSL／Docker、不新增独立浏览器引擎；已有环境按声明范围识别。
2. 没有聊天框；AI 主导识别和精确处理规划，经用户确认后调用客户端受限工具执行与复查，不持任意终端。模型配置在服务端，提示词源在客户端。
3. Clash 在应用内。后台托管方案、资源与配额，普通用户不填写订阅和代理参数；自助编辑范围是日常直连白名单。
4. 日常单 IP、Claude 单 IP、Claude 双 IP 三套方案不改语义。Claude 用 A，双 IP 的其他流量用 B，不轮换。
5. 同一套受管配置、同一个配额权威来源、同一个声明版本的路由分类器。不按模块另建配置副本、账本或过滤器。
6. 单次具体确认、首次范围授权的常规维护、预授权紧急保护，是三种不同授权。扫描和审计不是任意写入许可。
7. 当前 WRONG_ROUTE 或关键保护失效，由客户端程序立即按预授权保护，不等待 AI、后台、两小时归档或日报；动作与生效结果分开。手动应急上网走独立批准通道，Claude 不自动回退。
8. 项目记录、项目记忆和项目配置保留。正常账号资料按用户回答保留，受限账号资料识别后由用户确认处理；不自动裁定风控。
9. 多用户可共享上游订阅，但按独立用户身份计量与停用，不能只停订阅更新。配额由服务端执行；本机重装、模式切换或恢复不能重置账目。超额、故障和上报失败均不能用未知直连解围。
10. 每两小时归档；America/Los_Angeles 09:00 后首个可用唤醒补日报。非 PASS 也算交付；窗口、去重、旧 v2 分类及历史不改写规则遵守 FD-04。
11. 监控在用日志、当前审计窗口证据、已生成日报不进入普通默认清理；归档／审计本身不联网、不造测试流量、不改网、不删文件。
12. 日报明细默认留本地；后台默认只收用量汇总和最小事件，获授权的 AI 调用另发必要脱敏证据或汇总。秘密不进入模型、普通报告或公开仓库。

## 实施纪律

- 关键歧义会改变需求、授权、计量或验收时，先说明未知并澄清。不得猜测后一路自洽推进。
- 优先复用现成可用能力；采用前核对固定版本、许可与实际行为。不照搬上游的默认风险分或破坏性操作。
- 技术栈、供应商、API 路径、数据库和工期见待定决策，不从图片或偏好补成已定结论。
- 先实现可验证的小闭环，再扩大范围；不为预留扩展先造微服务、多级总线或万能 Agent。
- 只修改本任务涉及的原文和实现；提出需求变更必须显式说明，不用测试代码偷偷改变行为。
- 读到已有项目仓库时，以实际代码与运行证据核对现状。本包“未实施证明”不等于断言外部没有任何代码。

## 每次交接保留四项

本次涉及的 FD 编号；实际修改和未修改范围；运行了哪些检查及结果；仍未决定或未验证的条件。示例和计划不得记成测试 PASS。

评审只按可复现失败、明确违约或真实风险指出 BLOCK／MAJOR／MINOR；不以极端猜测无限增加要求。停止条件是本任务必要项关闭、验收可说明，未解决事项如实记录。

## 详细材料

[逻辑接口](DOCS/INTERFACES.md) · [综合验收](DOCS/ACCEPTANCE.md) · [待定决策](DOCS/OPEN_DECISIONS.md) · [安全边界](DOCS/SECURITY_PRIVACY.md)

当前新增决定见 [决策依据](DOCS/SOURCE_DECISIONS.md)；具体方案、支持矩阵和运行默认建议见 [完整方案](DOCS/V1_PLAN.md)。不要将建议值记录为 Owner 已批准，或将文档校验记成产品运行 PASS。
