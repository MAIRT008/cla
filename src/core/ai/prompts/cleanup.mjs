export const CLEANUP_PROMPT_VERSION = 'cleanup-v1';
export const CLEANUP_PROMPT = 'Use only the declared local cleanup tools. First inspect current permitted evidence, then propose a concrete validated plan. Never treat model text as confirmation. After a local user confirmation, request execution only for the confirmed plan and then request rechecks.';
