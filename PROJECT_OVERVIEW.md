# 项目说明：Claude 环境管理应用

> PRJ-00 · v1.3 · 2026-09-12。产品名称暂定，当前为直接保存在项目中的开发方案。

将 Owner 已用的本地清理、Clash 固定出口、监控归档经验变成普通用户能操作的应用。目标是检测、给出建议、确认后直接处理并验证，减少本方可控的环境问题。

用户只需扫描、回答账号是否正常、确认处理清单、选择已分配网络方案、管理日常直连白名单、查看用量与日报。管理员维护资源、个人额度、配置版本及模型服务。AI 主导识别和精确计划，在获确认范围内调用客户端工具执行；模型配置在服务端，提示词源在客户端。没有聊天框。

| 模块 | 用户取得的结果 |
|---|---|
| FD-01 本地检测清理 | 文件/字段/记录级建议，项目和正常账号保留，精确执行与复查 |
| FD-02 网络浏览器 | 网络事实、真实默认浏览器检测、可解释评分、确认后应用专用设置 |
| FD-03 内置 Clash | 三套后台配置的方案、自动维护、共享上游的个人额度与服务端独立停用 |
| FD-04 监测报告 | 清楚的本机/服务端用量、即时保护、只读归档、标准日报与 AI 分析 |

三套方案固定为日常单 IP、Claude 专用单 IP、Claude 专用双 IP。Claude 使用 A，双 IP 的其他业务使用 B；日常白名单允许直连，专用模式停用该白名单。保护规则优先，前置不是最终出口，固定不等于独享。免费/Pro/Max 的对应仅作为产品推荐，不是官方风险等级。

固定 A 故障时 Claude 保持阻断，用户可以主动启用独立应急上网入口以联系支持。配额按应用用户计算，某用户耗尽只停该用户，其他用户继续。不能依赖客户端自觉停用或撤销订阅下载链接来冒充服务端控制。

默认浏览器采用真实读取和页面诊断；流量统计不追求全部应用精准归属，未知如实显示。两小时归档与 America/Los_Angeles 09:00 后首个可用唤醒日报保留。AI 使用程序统计，不能改计数或回写历史。

首版围绕已有本机环境，不新建隔离工作台、WSL/Docker、虚拟机或浏览器引擎。建议首批 Windows 11 x64、Chrome/Edge 和已验证的 Claude 稳定版本，见 [支持范围](DOCS/RELEASE_SCOPE.md)。

完整使用流程、技术方案和失败处理见 [首版方案](DOCS/V1_PLAN.md)及[架构](ARCHITECTURE.md)。先按[复用计划](DOCS/REUSE_PLAN.md)对照桌面与 AI 的真实改造量，优先核实供应商原生配额；[开发计划](DOCS/IMPLEMENTATION_PLAN.md)保留原估时为待定型校正的参考，非已完成进度。[综合验收](DOCS/ACCEPTANCE.md)是后续产品验证入口。
