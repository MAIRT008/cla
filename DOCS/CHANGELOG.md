# 项目方案修改记录

## 当前文档入口与施工分工

2026-09-12。按 Owner 最新安排：GROK 与 Codex 5.3 共同施工，当前验收 Agent 负责独立验收与技术裁决，已同步项目 AGENTS、README、开发计划、完整方案和决策说明。早期 Claude Code 主施工安排不再适用于本项目。

明确根目录与 DOCS 内文件是当前实施依据，dist 的 ZIP 和展开目录仅为历史导出快照，无需解压即可工作。本次只更新源文档和校验索引，没有重新打包，也不改写此前导出的版本或审计记录；产品未开始施工。

## v1.3 优化修订

2026-09-12。按 Owner 要求，将六项优化写回正式方案并进行独立一致性审核。桌面采用 CVR 裁剪与轻壳净改造量对照；AI 采用现成模型 SDK 加窄工具循环与 Pi 对照，确认后一次本地批量执行；配额优先供应商原生用户能力；应急优先合适的已安装第二浏览器；任务按实际依赖推进，数值先测后定。未指定最终仓库胜出。

同步完整方案、架构、复用计划、接口、运行行为、待定决策、开发计划、验收和入口说明。四份 FD 业务基线及原编号保持原样，历史审计与 v1.2 ZIP 保留。原 6—8 周降为明确待选型校正的参考，没有在缺少集成工时的情况下重新承诺缩短。

校验与打包工具只更新本包版本到 v1.3；索引、图源、哈希和本地 ZIP 按实际修订重新生成。结构检查、独立文档审核、候选实测和产品放行分别记录；只有文档核对通过才能给文档通过结论，尚未做过的运行验证继续标 UNVERIFIED。

## v1.2 方案补齐（历史记录）

日期：2026-09-12。任务：先补齐方案，再制定开发计划并重估工期；参考 Owner 指定的历史对话和当前六项澄清。

## 修改

修订 README、PROJECT_OVERVIEW、ARCHITECTURE、项目 AGENTS 和四份 FD 的相关条款。保留 FD 原编号和详细场景；修正 AI 权限/部署、默认浏览器、共享配额、应急入口、流量归属和 AI 日报的当前口径。四份 Mermaid 与架构正文同步生成。

补齐原来缺失的 RELEASE_SCOPE、DOCUMENT_INDEX、INTERFACES、IMPLEMENTATION_PLAN、SECURITY_PRIVACY、ACCEPTANCE、OPEN_DECISIONS，并增加完整首版方案、运行行为、复用计划和决策依据。提供机器索引、任务映射、来源哈希及真正可运行的文档校验工具。

以阶段任务重估为 23—35 个有效工作日，加 3—5 日缓冲；建议完整 V1 小范围试用按 6—8 周计划。旧审计中的 16—24 周是当时较宽假设下估计，保留原审计，不回写成“当时没有问题”。

## 保留与未修改

修改前八份 Markdown 快照在 history/pre-v1.2-20260912，附哈希。原五份 Word、一张 PNG、零字节 v1.1 ZIP 保留原样并降为历史材料。本轮有效 ZIP 单独生成，未用空包冒充交付。

未修改其他 Agent 控制面、本机 Claude 数据、现用 Clash、网络配置和历史脚本；未安装软件、部署服务、发实际模型任务或运行生产故障试验。当前目录不是 Git 仓库；未 commit、push 或发布。

## 验证与结论边界

文档校验检查当前包所声明的本地链接、FD/验收/规则编号、机器索引行号和哈希、任务覆盖、四份图源与正文一致。实际运行输出见 [校验结果](index/document-validation.json)；本地 ZIP 清单、CRC 和哈希记录保存在 DOCS/index/package-validation.json，该记录位于 ZIP 外以避免文件自引用。

原审计“引用缺失材料”的交付缺陷在当前文档包内按这些实际检查复核；原审计报告保留为历史证据，不能用当前补齐否认旧状态。

供应商独立用户接口、受管节点拓扑、系统断流保护、真实浏览器适配、AI 实际清理质量及性能仍待 P0 和后续测试。当前完成的是可评审开发方案与任务计划，不是软件运行验收。
